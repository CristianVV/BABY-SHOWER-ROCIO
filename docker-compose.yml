version: "3.8"

services:
  db:
    image: postgres:16-alpine
    container_name: babyshower-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: babyshower
    # NO PORTS EXPOSED - internal only to avoid conflict with tradingops-timescale on 5433
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: babyshower-app
    restart: unless-stopped
    ports:
      - "3003:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      NODE_ENV: production
      NEXT_PUBLIC_APP_URL: https://rociovp.com
      NEXT_PUBLIC_WHATSAPP_NUMBER: "+34649225590"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - uploads:/app/public/uploads

volumes:
  postgres_data:
  uploads:
